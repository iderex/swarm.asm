# Dependabot — weekly maintenance for the GitHub Actions SHA pins.
#
# The nuget ecosystem is added by the PR that introduces the tests/ harness;
# configuring it before a manifest exists only produces noise.

version: 2

updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "05:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 5
    groups:
      actions-minor-and-patch:
        update-types:
          - minor
          - patch
